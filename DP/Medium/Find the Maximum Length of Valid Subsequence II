import java.util.*;

class Solution {
    public int maximumLength(int[] nums, int k) {
        int n = nums.length, answer = 0;
        int[][] dp = new int[n][k];
        for(int i = 0; i < n; ++i) {
            for(int j = 0; j < i; ++j) {
                int sum = (nums[i] + nums[j]) % k;
                dp[i][sum] = dp[j][sum] + 1;
                answer = Math.max(answer, dp[i][sum]);
            }
        }
        return answer + 1;
    }
}
